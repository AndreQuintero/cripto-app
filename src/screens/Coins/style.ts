import styled from "styled-components/native";
import { Layout, Text as KText } from '@ui-kitten/components'


export const View = styled(Layout)`
    flex: 1;
    justify-content: center;
    align-items: center
`

export const Text = styled(KText)`
    font-size: 24px;
`
